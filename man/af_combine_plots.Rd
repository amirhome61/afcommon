% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/af_basic_plots.R
\name{af_combine_plots}
\alias{af_combine_plots}
\title{Combine plots in a grid with optional title, subtitle, note, and legend options}
\usage{
af_combine_plots(
  plot_list,
  ncol = 2,
  main_title = NULL,
  subtitle = NULL,
  note = NULL,
  common_legend = FALSE,
  legend_position = "right",
  external_legend = NULL,
  legend_labels = NULL,
  row_spacing = 0.5,
  col_spacing = 0.5
)
}
\arguments{
\item{plot_list}{A list of ggplot objects to be combined}

\item{ncol}{Integer. Number of columns in the grid layout (default: 2)}

\item{main_title}{Character string. Main title for the combined plot (default: NULL)}

\item{subtitle}{Character string. Subtitle for the combined plot (default: NULL)}

\item{note}{Character string. Note to be placed at the bottom (default: NULL)}

\item{common_legend}{Logical. Whether to extract and use a common legend from plots (default: FALSE)}

\item{legend_position}{Character. Position of the common legend: "right", "bottom", "top", "left" (default: "right")}

\item{external_legend}{List. List of pairs defining color and line type for custom legend (default: NULL)
Format: list(c("color", "linetype"), c("color", "linetype"), ...)
Example: list(c("red", "dashed"), c("blue", "dotted"), c("green", "solid"))}

\item{legend_labels}{Character vector. Labels for external legend items (default: NULL, uses "Item 1", "Item 2", etc.)}

\item{row_spacing}{Numeric. Spacing between rows in the grid layout in lines (default: 0.5)}

\item{col_spacing}{Numeric. Spacing between columns in the grid layout in lines (default: 0.5)}
}
\value{
Invisibly returns the combined plot object
}
\description{
This function arranges a list of ggplot objects in a grid layout and can
optionally add a main title, subtitle, note, extract a common legend from plots,
or create a custom external legend from color and line type specifications.
Spacing between plots can be controlled using row_spacing and col_spacing parameters.
}
\examples{
# Basic usage
af_combine_plots(list(plot1, plot2), ncol = 2)

# With common legend from plots
af_combine_plots(list(plot1, plot2),
                 main_title = "Main Title",
                 common_legend = TRUE)

# With custom external legend
af_combine_plots(list(plot1, plot2),
                 main_title = "Analysis Results",
                 external_legend = list(c("red", "dashed"), c("blue", "dotted"), c("green", "solid")),
                 legend_labels = c("Treatment A", "Treatment B", "Control"))

# With reduced spacing between plots
af_combine_plots(list(plot1, plot2, plot3, plot4),
                 ncol = 2,
                 main_title = "Tight Layout",
                 row_spacing = 0.1,
                 col_spacing = 0.1)

}
