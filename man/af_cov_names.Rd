% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/af_stargazer.R
\name{af_cov_names}
\alias{af_cov_names}
\title{Create Display Names for Regression Coefficients}
\usage{
af_cov_names(df, models, display_names)
}
\arguments{
\item{df}{(data.frame) The data frame used in the regression models}

\item{models}{(model object or list) A single fitted model or list of models}

\item{display_names}{(named character vector) Mapping of variable names to display labels}
}
\value{
(character vector) A character vector of formatted display names in the order of coefficients, with categorical levels in brackets and interactions formatted with multiplication symbols
}
\description{
Transforms technical regression coefficient names into human-readable display labels
for tables and plots. Maps variable names to display names, formats categorical variable
levels in brackets, converts interaction terms using multiplication symbols, and renames
the intercept. Preserves the ordering from stargazer output.
}
\examples{
\dontrun{
# Example usage
model <- lm(mpg ~ factor(cyl) + wt + factor(gear):wt, data = mtcars)
display_names <- c(
 "factor(cyl)" = "Cylinders",
 "wt" = "Weight",
 "factor(gear)" = "Gears"
)
af_cov_names(mtcars, model, display_names)
}
}
