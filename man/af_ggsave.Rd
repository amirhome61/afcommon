% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/af_basic_plots.R, R/af_utils.R
\name{af_ggsave}
\alias{af_ggsave}
\title{Save ggplot with Custom Defaults}
\usage{
af_ggsave(
  filename,
  plot = last_plot(),
  units = "mm",
  width = 180,
  height = 90,
  dpi = 600,
  display = TRUE
)

af_ggsave(
  filename,
  plot = last_plot(),
  units = "mm",
  width = 180,
  height = 90,
  dpi = 600,
  display = TRUE
)
}
\arguments{
\item{filename}{The file path to save the plot}

\item{plot}{The ggplot object to save (default: last_plot())}

\item{units}{Units for width and height (default: "mm")}

\item{width}{Width of the plot (default: 180)}

\item{height}{Height of the plot (default: 90)}

\item{dpi}{Dots per inch for the saved plot (default: 600)}

\item{display}{Logical, whether to display the plot after saving (default: TRUE)}
}
\value{
NULL (called for side effects of saving file and optionally displaying plot)

None. The function saves the plot to a file and optionally displays it.
}
\description{
Saves a ggplot object to a file with preset dimensions and resolution suitable for
publication-quality figures. Optionally displays the plot before saving. Wraps the
ggplot2::ggsave function with convenient default parameters.

This function saves a ggplot object to a file with specified width, height, and DPI.
It also provides an option to display the plot after saving.
}
\examples{
p <- ggplot(mtcars, aes(x = mpg)) + geom_histogram()
af_ggsave("myplot.png", p)

p <- ggplot(mtcars, aes(x = wt, y = mpg)) + geom_point()
af_ggsave("mtcars_plot.png", plot = p, width = 200, height = 100, dpi = 300)

}
